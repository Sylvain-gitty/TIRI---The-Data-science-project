# Spec linter — findings, each with the number that prices it

Generated by `scripts/spec_linter.py --specs all` over **34 specs**. $0, no LLM, no labels, deterministic. This is §6 of [`wf_spec_quality_answer.md`](wf_spec_quality_answer.md) as code — read that first for why there is no single quality **score**.

## 0. How to read any number here

**Severities.** `blocker` = a field the measurements say is load-bearing is missing or degenerate. `warn` = a defect with a measured cost that a fitted model can partly work around. `note` = certainly a mistake, priced as near-free, surfaced but not worth blocking on.

**`kind`.** `exact` checks are field counts, emptiness and set intersections — no judgement in them. `heuristic` checks are hand-built patterns for *"this prose is waffle"* and *"this prose is an instruction"*. **The costs are measured either way; two of the detectors are not.** A spec could be waffle in a way those patterns miss.

**Every price is ROC-AUC**, quoted as `set A / set B / TIRI` — three surfaces at 2.19%, 1.87% and 57.6% positive. **0.500 is a coin flip** and **a gap under 0.03 is not established** (`CONTEXT.md` §5).

**What is deliberately not checked:** empty `problem_statement`, empty `domain_*`, empty `terms_exclude`. They measure **0.000 at zero labels and ≤0.009 fitted on all three surfaces**. Warning about them would be warning about nothing, and a linter that fires on healthy input teaches the analyst to ignore it.

**Label which critic is speaking.** The prices are hard measurements off held-out rows. Whether a *pattern* correctly identifies waffle is a judgement call, flagged as `heuristic`.

## 1. Summary

- **5 findings** across 4 of 34 specs: 5 warn
- **30 specs are clean**: bench/nykvist_evcharging, bench/roadfreight_metareview, bench/synergy_appenzeller-herzog_2019, bench/synergy_bos_2018, bench/synergy_brouwer_2019, bench/synergy_chou_2003, bench/synergy_chou_2004, bench/synergy_donners_2021, bench/synergy_hall_2012, bench/synergy_jeyaraman_2020, bench/synergy_leenaars_2019, bench/synergy_leenaars_2020, bench/synergy_meijboom_2021, bench/synergy_menon_2022, bench/synergy_moran_2021, bench/synergy_muthu_2021, bench/synergy_nelson_2002, bench/synergy_oud_2018, bench/synergy_radjenovic_2013, bench/synergy_sep_2021, bench/synergy_smid_2020, bench/synergy_van_de_schoot_2018, bench/synergy_van_der_valk_2021, bench/synergy_van_der_waal_2022, bench/synergy_van_dis_2020, bench/synergy_walker_2018, bench/synergy_wassenaar_2017, bench/synergy_wolters_2018, tiri/soil_microbiome, tiri/tech_forecasting
- Most findings on a single spec: **2** (`tiri/solar_leo`)

## 2. Which checks fire, and where

| check       | severity | n | examples                                                           |
|-------------|----------|---|--------------------------------------------------------------------|
| must_thin   | warn     | 4 | tiri/carbon_capture, tiri/cement_binders, tiri/ner, tiri/solar_leo |
| nice_single | warn     | 1 | tiri/solar_leo                                                     |

## 3. Every finding

| use_case            | check       | severity | kind  | what                                                                           | fix                                                                                    |
|---------------------|-------------|----------|-------|--------------------------------------------------------------------------------|----------------------------------------------------------------------------------------|
| tiri/carbon_capture | must_thin   | warn     | exact | There are fewer must-include terms than the measured recommendation.           | Consider adding a few more, but treat this as advice rather than a defect.             |
| tiri/cement_binders | must_thin   | warn     | exact | There are fewer must-include terms than the measured recommendation.           | Consider adding a few more, but treat this as advice rather than a defect.             |
| tiri/ner            | must_thin   | warn     | exact | There are fewer must-include terms than the measured recommendation.           | Consider adding a few more, but treat this as advice rather than a defect.             |
| tiri/solar_leo      | nice_single | warn     | exact | There is exactly one nice-to-have term, which is nearly as bad as having none. | Add ≥5 discriminative phrases. Going from 1 to 5 buys far more than going from 0 to 1. |
| tiri/solar_leo      | must_thin   | warn     | exact | There are fewer must-include terms than the measured recommendation.           | Consider adding a few more, but treat this as advice rather than a defect.             |

## 4. The prices

| check                        | severity | kind      | measured cost                                                                                                                                                                                                                                                                                                                |
|------------------------------|----------|-----------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| objective_missing            | blocker  | exact     | The objective is worth +0.151 / +0.263 / +0.100 ROC-AUC above a coin flip at zero labels (set A / set B / TIRI). Deleting it does not degrade that feature — it kills it, pinning it to exactly 0.500.                                                                                                                       |
| only_name                    | blocker  | exact     | The worst variant measured on all three surfaces: −0.105 / −0.045 / −0.087 after 60 labels, and −0.151 / −0.263 / −0.118 at zero. It is also what `embedding_utils.get_use_case_text` falls back to, so it is what ships today.                                                                                              |
| nice_empty                   | blocker  | exact     | Worth +0.105 / +0.215 / +0.118 at zero labels — and on TIRI's own use cases it is the **largest** cold-start effect of any field, above the objective. One term recovers only about a quarter of that (see `nice_single`), so this is not a formality.                                                                       |
| must_single                  | blocker  | exact     | −0.103 / −0.175 / −0.039 at zero labels. Note the TIRI figure is much smaller, because its lists are already short — see `must_thin`.                                                                                                                                                                                        |
| nice_single                  | warn     | exact     | Measured specifically to price this check. One term costs −0.168 (set B) / −0.069 (TIRI) at zero labels against **−0.215 / −0.118** for deleting the field outright — so a single term recovers only about a quarter of the field's value. In absolute terms on set B: a full list scores 0.715, one term 0.547, none 0.500. |
| objective_generic            | warn     | heuristic | Replacing real prose with filler costs −0.177 / −0.169 / −0.101 at zero labels — most of what the field is worth. On two of three surfaces it drives the feature to or below a coin flip (0.473 set A, 0.499 TIRI); on set B it does not, so the effect is real but surface-dependent.                                       |
| objective_instruction_shaped | warn     | heuristic | A vague one-liner of this shape costs −0.209 / −0.194 / −0.109 at zero labels. The cold-start feature matches your text against paper abstracts, so text that describes your intent rather than the papers has nothing to match.                                                                                             |
| terms_generic                | warn     | exact     | Padding term lists with this vocabulary costs −0.072 / −0.031 / −0.063 after 60 labels, worse than leaving the field **empty** (−0.043 / −0.010 / −0.032). It is the most consistent after-labels defect measured, and none of the 34 real specs contains a single one of these words.                                       |
| must_thin                    | warn     | exact     | ⚠️ **Recommendation, not a measured defect at this size.** 5–8 is what pays on the benchset corpus; the *measured* penalty is for dropping to one term, and on TIRI's own use cases that penalty is only −0.039. This check fires on 4 of TIRI's 6 specs and 0 of 28 benchset briefs.                                        |
| exclude_in_must              | note     | exact     | Priced as **near-free**: −0.014 / −0.009 / −0.001 for a matcher, and **+0.016** for an LLM reader, which is to say a reader did marginally better with it. Worth surfacing because it is certainly a mistake, but it is not worth blocking on.                                                                               |

## 5. When more than one fires

⚠️ **Findings compound; they do not cancel.** Three prose × term pairs measured on two surfaces, and in **6 of 6 cells the pair cost more than the sum of its parts** (excess 0.008–0.020, and exactly −0.020 on both surfaces for fluff + keyword-flood). **There is no rescue effect** — a strong objective does not license a lazy term list.

So a spec tripping both a prose check and a term check is in disproportionately worse shape than adding the two numbers suggests. 🟡 Each individual excess is inside the 0.03 noise floor, so read this as a **direction, not a quantity**: the evidence is 6 of 6 same-sign across two independent surfaces.

## 6. When is a spec done?

**Before any labels:** when this linter is silent. That is a necessary condition, not a sufficient one — it only catches the failure modes that were measured.

**After labelling:** when your own brief beats a **deliberately wrong** brief on your own corpus (the shuffled-brief control, `build_lexical_features(df, brief_map=…)`), at the measured gate of ≥20 positives and ≥50 negatives (`wf_label_budget_shape.md`). That is the only general test of brief quality that has passed on every surface tried: AUC 0.777 → 0.498 lexically, and −0.282 for an LLM reader.

⚠️ **The honest asymmetry:** the linter is label-free but only catches known failure modes. The completion test is general but needs labels. **Nothing validated is both**, and every attempt to build something that is (§5 of the answer) failed.
