# Core data handling
pandas>=2.2.0
pyarrow>=16.0.0        # parquet read/write for pandas
numpy>=2.0.0

# Modelling
scikit-learn>=1.4.0

# Visualisation (latent-space comparison plots)
matplotlib>=3.8.0

# Local sentence embeddings (ONNX runtime, no GPU/PyTorch/API key needed) —
# same library the academic_research_agent uses, so vectors/behaviour are comparable.
fastembed>=0.8.0

# Local sentence embeddings for models fastembed doesn't carry (e.g. Specter, the
# domain-specific academic-paper model) — still fully local/offline, just PyTorch-backed.
sentence-transformers>=3.0.0

# Local NER (scripts/compare_ner_models.py) — fully offline, no PyTorch requirement for
# the small pipeline. After installing, fetch the model once with:
#   python -m spacy download en_core_web_sm
spacy>=3.7.0

# Notebooks (optional, week 1 EDA)
jupyter>=1.0.0

# Interactive scatter (notebooks/eda/wf_data_enrich.ipynb citations-vs-age plot) — the
# only chart in the repo that needs real hover/zoom, so it's the one chart on plotly
# rather than matplotlib.
plotly>=5.24.0
