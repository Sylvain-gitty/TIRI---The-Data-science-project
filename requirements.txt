# Core data handling
pandas>=2.2.0
pyarrow>=16.0.0        # parquet read/write for pandas
numpy>=2.0.0

# Modelling
scikit-learn>=1.4.0

# Visualisation (latent-space comparison plots)
matplotlib>=3.8.0

# Local sentence embeddings (ONNX runtime, no GPU/PyTorch/API key needed) —
# same library the academic_research_agent uses, so vectors/behaviour are comparable.
fastembed>=0.8.0

# Local sentence embeddings for models fastembed doesn't carry (e.g. Specter, the
# domain-specific academic-paper model) — still fully local/offline, just PyTorch-backed.
sentence-transformers>=3.0.0

# Local NER (scripts/compare_ner_models.py) — fully offline, no PyTorch requirement for
# the small pipeline. After installing, fetch the model once with:
#   python -m spacy download en_core_web_sm
spacy>=3.7.0

# Notebooks (optional, week 1 EDA)
jupyter>=1.0.0

# Interactive scatter (notebooks/eda/wf_data_enrich.ipynb citations-vs-age plot) — the
# only chart in the repo that needs real hover/zoom, so it's the one chart on plotly
# rather than matplotlib.
plotly>=5.24.0

# Hosted embedding models via OpenRouter (notebooks/eda/wf_embedding_model_bakeoff.ipynb) —
# requests calls OpenRouter's /embeddings endpoint, python-dotenv loads OPENROUTER_API_KEY
# from the repo-root .env (gitignored) rather than requiring it as a shell env var.
requests>=2.31.0
python-dotenv>=1.0.0

# GPU-backed inference for 4 heavier HF models (SPECTER2, QZhou-Embedding,
# Jasper-Token-Compression-600M, Qwen3-Embedding-4B) via scripts/modal_embeddings.py —
# too slow/impractical to run locally on CPU. Needs `modal token new` / an existing
# ~/.modal.toml to authenticate; see that file's docstring for deploy instructions.
modal>=1.0.0

# SYNERGY systematic-review dataset (notebooks/eda/wf_synergy_validation.ipynb) — an
# independent external validation benchmark, see reports/wf_ensemble_report.md action
# item #2. CC0-licensed, no auth needed; Dataset(name).to_frame() downloads on demand.
synergy-dataset>=1.2

# Advanced tree ensemble candidate (notebooks/pipelines/sf_catboost_fold_pipeline.ipynb,
# notebooks/pipelines/wf_ensemble_fold_pipeline.ipynb) — ordered boosting specifically
# targets the target-leakage-during-training that's already been observed on this dataset
# (RandomForestClassifier/HistGradientBoostingClassifier both hit train AUC 1.000 under
# LOGO); native missing-value/categorical handling, monotonic constraints, mature SHAP
# tooling; CPU-only, no GPU required.
catboost>=1.2.0

# Bayesian (TPE) hyperparameter search in notebooks/main/07_advanced_catboost.ipynb and
# 08_ensemble_pooled.ipynb — both do `import optuna` unguarded in their first cell, so
# without this they fail before reaching any modelling code.
optuna>=3.6.0

# Pulled in transitively by scikit-learn/catboost today, but imported directly by the
# notebooks (scipy for the significance tests, joblib to parallelize fold-level fits) —
# named explicitly so a dependency change upstream can't silently break them.
scipy>=1.13.0
joblib>=1.4.0
